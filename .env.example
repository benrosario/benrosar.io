# Optional live demo. Both settings are read at runtime.
# Leave unset to show only the project overview.
# The API must have the same Google client ID configured.
# SIERRA_API_URL=https://your-api-host
# SIERRA_GOOGLE_CLIENT_ID=123456-example.apps.googleusercontent.com
# Never add the Discord bot token, Google client secret, or model API keys here.
